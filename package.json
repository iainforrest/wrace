{
  "name": "wrace",
  "targets": {
    "": {
      "source": "./index.html",
      "distDir": "dist/"
    },
    "demo": {
      "source": "./demo/index.html",
      "distDir": "dist/demo"
    }
  },
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build --no-source-maps && cp CNAME ./dist && cp .nojekyll ./dist",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.7.0",
    "@parcel/transformer-webmanifest": "^2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}
